# Django environment example
DJANGO_ENV=local
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=1
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0,hamster-desktop,hamster-dektop

# Database (PostgreSQL)
POSTGRES_DB=cadma
POSTGRES_USER=cadma
POSTGRES_PASSWORD=cadma
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Optional: directly specify DATABASE_URL instead (overrides POSTGRES_* if set)
# DATABASE_URL=postgres://cadma:cadma@db:5432/cadma
